# AutoEnterVoiceChannel 🔊

A BetterDiscord plugin that automatically joins a specific Voice Channel when Discord starts or if you are disconnected by mistake.

## ✨ Features
- **Auto Join**: Automatically joins the specified voice channel without any intervention.
- **Full Customization**: Configure the channel, check interval, and startup delay.

## 📥 Installation
1. Download `AutoEnterVoiceChannel.plugin.js`.
2. Place it in the BetterDiscord plugins folder:
   - Path usually: `%appdata%/BetterDiscord/plugins`
3. Enable the plugin from Discord settings.

## ⚙️ Configuration
Open the plugin settings within Discord to adjust the following options:

### 1. Channel ID
- The ID of the voice channel you want to join.
- **How to get it?**: Enable Developer Mode in Discord, right-click the voice channel, and select "Copy ID".

### 2. Check Interval
- Time in seconds for the plugin to check if you are in the channel.
- **Default**: 10 seconds.

### 3. Initial Delay
- Time the plugin waits after Discord starts before attempting to join.
- Useful to allow Discord to fully load.
- **Default**: 45 seconds.

## 📝 Notes
- If the plugin doesn't work, ensure the Channel ID is correct and you have permission to join that channel.

---

# AutoEnterVoiceChannel 🔊 (العربية)

إضافة لبرنامج BetterDiscord تقوم بإدخالك تلقائياً إلى قناة صوتية (Voice Channel) محددة عند تشغيل ديسكورد أو عند الخروج منها عن طريق الخطأ.

## ✨ المميزات | Features

- **الدخول التلقائي**: يدخلك الروم الصوتي المحدد تلقائياً دون أي تدخل منك.
- **تخصيص كامل**: يمكنك تحديد القناة، وقت الفحص، ووقت الانتظار عند البدء.

## 📥 التثبيت | Installation

1. حمّل ملف `AutoEnterVoiceChannel.plugin.js`.
2. ضعه في مجلد إضافات BetterDiscord:
   - المسار عادة يكون: `%appdata%/BetterDiscord/plugins`
3. قم بتفعيل الإضافة من داخل إعدادات ديسكورد.

## ⚙️ الإعدادات | Configuration

افتح إعدادات الإضافة من داخل ديسكورد لتعديل الخيارات التالية:

### 1. Channel ID (معرف القناة)
- المعرف الخاص بالقناة الصوتية التي تريد الدخول إليها.
- **كيف تجيبه؟**: فعل وضع المطور (Developer Mode) في ديسكورد، ثم اضغط كليك يمين على الروم الصوتي واختر "Copy ID".

### 2. Check Interval (فترة الفحص)
- الوقت بالثواني الذي تقوم فيه الإضافة بالتحقق مما إذا كنت داخل الروم أم لا.
- **الافتراضي**: 10 ثواني.

### 3. Initial Delay (التأخير الأولي)
- الوقت الذي تنتظره الإضافة بعد تشغيل ديسكورد لأول مرة قبل محاولة الدخول.
- هذا مفيد لمنح ديسكورد وقتاً للتحميل الكامل.
- **الافتراضي**: 45 ثانية.

## 📝 ملاحظات
- إذا لم تعمل الإضافة، تأكد من صحة الـ Channel ID وأن لديك صلاحية دخول لتلك القناة.
